ls > $eeee (ambiguous redirect)

ls > "" (must return prompt after display error msg)

ls | << ziko (heredoc must be opened, not display syntax error, he works correctly when it's just one cmd like (<< ziko)) , but with a pipe before no

heredoc , should not expand the filename in this case
        example : cat << $USER, test it in bash to understand

export var=a ; export $var=test ; echo $var $a (sometimes works , sometimes not)

export $var=test (i found that export created declare -x ="test") -> bash: export: `=test': not a valid identifier

export "" test=a -> (bash: export: `': not a valid identifier) and exit-status must be 1, not 0

echo ~ -> must print $HOME

. -> exit-status must be 2, not 1

.. -> exit-status must be 127, not 1

"" -> command not found, and exit-status is 127 not 1

echo "\s", echo "\\s" -> should print \s

echo "12\"" -> should print 12"

echo "bip | bip ; coyotte > < \" " -> not like bash

echo bonjour > $test -> ambigue redire , exit-status must be 1 

echo \>

echo $USER$var\$USER$USER\$USERtest$USER -> not like bash

cat << test << test1 << test2 << test3 << test4 << test5 << test6 << test7 << test8 << test9 << test10 << test11 << test12 << test13 << test14 << test15 << test16 << test17 
-> maximum here-document count exceeded is 17
