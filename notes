ls > $eeee (ambiguous redirect)

ls > "" (must return prompt after display error msg)

ls | << ziko (heredoc must be opened, not display syntax error, he works correctly when it's just one cmd like (<< ziko)) , but with a pipe before no

heredoc , should not expand the filename in this case
        example : cat << $USER, test it in bash to understand

echo $? must exit-status be 127 when i execute ./jhgasd (bcuz this file does not exist)

cmd not found like 'jhdsfkj' or is any file does not exist -> exit-status must be 127

ls hello (cannot access 'hello': No such file or directory) -> exit-status must be 2

export var=a ; export $var=test ; echo $var $a (sometimes works , sometimes not)